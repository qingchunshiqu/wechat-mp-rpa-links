# Changelog

## v0.1.0 - 2026-06-11

### Added

- 支持按公众号名称和微信号采集历史文章链接。
- 支持下载文章 HTML 与图片资源到本地目录。
- 支持 `--since`、`--until` 和 `--scan-limit` 控制时间范围与扫描页数。
- 支持读取 `crawl_tasks.json` 执行每日批量抓取任务。
- 支持通过 `--browser-channel chrome/msedge` 使用本机浏览器运行。

### Changed

- 完善公开文档、风险说明和本地运行配置示例。
